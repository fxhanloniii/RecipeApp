<%- include('./partials/header.ejs', {pageTitle: "Recipe.cuisine"}) %>

<div class="recipes">
    <% for(let i=0; i < 20; i++) { %>
    <div class="recipeDiv">
        <h3><%= recipes[i].recipe_name %></h3>
        <img src="<%= recipes[i].image %>">
    </div>
    <% } %>
</div>





<%- include('./partials/footer.ejs') %>